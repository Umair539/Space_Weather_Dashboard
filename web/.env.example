# Base URL of the FastAPI caching layer. Defaults to http://localhost:8000,
# which matches `run_api` and the docker/api container's default PORT, so
# local development needs no .env file at all.
# VITE_API_BASE_URL=http://localhost:8000
